CIFAR_100_Image_Classification/
├── main.py               # entry point, UI orchestration only
├── config.py             # constants, paths, thresholds
├── classes.py            # CIFAR-100 class & superclass data
├── model.py              # load model, preprocess, predict
├── styles.py             # all CSS
└── components.py         # reusable HTML/UI render functions

# install the required libraries/packages using requirements file
pip install -r requirements.txt